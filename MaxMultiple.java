public class MaxMultiple {
    int solution(int divisor, int bound) {
        //divide bound by divisor. multiply that by divisor to get quotient with no remainder
    return (bound / divisor) * divisor;
    }
    
}
